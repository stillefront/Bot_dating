extends layout

block content

    p.text-center= title

    .chat
        .chat-box
    
    .chat-user
        form.form-inline#form-chat(action="")
            button.btn.btn-default(type="submit", name="button", ) Stop   